{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debuguear Juego de Valu",
            "url": "http://localhost:5500/index.html", // O la URL de tu Live Server
            "webRoot": "${workspaceFolder}"
        }
    ]
}